// ConsoleApplication11.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"


int _tmain(int argc, _TCHAR* argv[])
{
	int a[10] = {1, 1};
	for (int i = 0; i < 10; ++i)
	{
		printf("\t%d\n", a[i]);
	}
	//printf("%d\n", a[i]);
	printf("\n");
	return 0;
}

